/**
 * Default export is the light palette for legacy imports.
 * In components, use `useTheme().colors` so dark mode applies.
 */
export { lightColors, darkColors } from '../theme/themes';
import { lightColors } from '../theme/themes';

export default lightColors;
